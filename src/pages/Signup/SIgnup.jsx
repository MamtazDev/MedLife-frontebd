import React from "react";
import { Link } from "react-router-dom";

const SIgnup = () => {
  return (
    <section>
      <h1>This is sigup page</h1>

      <Link to="/login">Go to login page</Link>
    </section>
  );
};

export default SIgnup;
